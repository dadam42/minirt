#include "minirt.h"
t_object	*minirt_add_rtsquare(t_minirt *minirt
							, t_filert_parsed_obj *parsed)
{
	(void)minirt; (void)parsed;
	return (minirt_ok);
}
t_object	*minirt_add_rttriangle(t_minirt *minirt
							, t_filert_parsed_obj *parsed)
{
	(void)minirt; (void)parsed;
	return (minirt_ok);
}
